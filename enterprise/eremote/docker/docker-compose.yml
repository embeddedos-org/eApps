version: '3.8'
services:
  eremote:
    build: .
    container_name: eos-eremote
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - EOS_LICENSE_KEY=${EOS_LICENSE_KEY}
    volumes:
      - eremote_data:/app/data
    networks:
      - eos-enterprise
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  eremote_data:

networks:
  eos-enterprise:
    external: true
