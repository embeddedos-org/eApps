if(EAPPS_PLATFORM_EOS)
    set(PLATFORM_SRC src/platform_eos.c)
elseif(WIN32)
    set(PLATFORM_SRC src/platform_win32.c)
elseif(EMSCRIPTEN)
    set(PLATFORM_SRC src/platform_web.c)
else()
    set(PLATFORM_SRC src/platform_linux.c)
endif()

add_library(eapps_platform STATIC ${PLATFORM_SRC})
target_include_directories(eapps_platform PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
target_link_libraries(eapps_platform PUBLIC eapps_common)
