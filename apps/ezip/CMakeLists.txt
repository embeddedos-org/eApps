add_library(ezip STATIC ezip.c)
target_include_directories(ezip PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(ezip PUBLIC eapps_ui)
if(EAPPS_BUILD_STANDALONE)
    add_executable(ezip_standalone ezip.c)
    target_link_libraries(ezip_standalone PRIVATE ezip eapps_port)
endif()